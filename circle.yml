machine:
  node:
    # Circle will use node v4.0.0
    version: 7.4.0

dependencies:
  override:
    - yarn
    - yarn add node-sass --force
test:
  pre:
    - mv .env.example .env
  override:
    - yarn test
