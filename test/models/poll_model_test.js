var chai = require('chai');
var should = chai.should();
var models = require('require.all')('../models')
var factory = require('../factories')

require('../test_config')

describe('Poll Model', function() {

  it('has a user', function(done) {
    var poll =
  })

});
